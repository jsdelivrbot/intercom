import { SortByLatestPipe } from './sort-by-latest.pipe';

describe('SortByLatestPipe', () => {
  it('create an instance', () => {
    const pipe = new SortByLatestPipe();
    expect(pipe).toBeTruthy();
  });
});
